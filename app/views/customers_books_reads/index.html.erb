<h1>Listing customers_books_reads</h1>

<table>
  <thead>
    <tr>
      <th>Customer</th>
      <th>Book</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @customers_books_reads.each do |customers_books_read| %>
      <tr>
        <td><%= customers_books_read.customer_id %></td>
        <td><%= customers_books_read.book_id %></td>
        <td><%= link_to 'Show', customers_books_read %></td>
        <td><%= link_to 'Edit', edit_customers_books_read_path(customers_books_read) %></td>
        <td><%= link_to 'Destroy', customers_books_read, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Customers books read', new_customers_books_read_path %>
